---
import { t } from '../../lib/i18n';

export interface Props { minutes: number; }
const { minutes } = Astro.props;
---

<span title={t('article.reading_time_title')}>{t('article.reading_time', { Count: minutes })}</span>
